$("ul.errors").html("");
<% if @landlord.errors.any? %>
  <% @landlord.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".landlords").html("<%= escape_javascript(render 'index') %>");
  $("#landlord-modal").modal("hide");
 
<% end %>