<%= form_with(model: customer_type, local: true) do |form| %>
  <% if customer_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_type.errors.count, "error") %> prohibited this customer_type from being saved:</h2>

      <ul>
      <% customer_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :customer_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :customer_type_description %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :customer_type_status %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :customer_type_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
