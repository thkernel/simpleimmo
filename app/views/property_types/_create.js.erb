$("ul.errors").html("");
<% if @property_type.errors.any? %>
  <% @property_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".property-types").html("<%= escape_javascript(render 'index') %>");
  $("#property-type-modal").modal("hide");
 
<% end %>