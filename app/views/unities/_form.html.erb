<%= form_with(model: unity, local: true) do |form| %>
  <% if unity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unity.errors.count, "error") %> prohibited this unity from being saved:</h2>

      <ul>
      <% unity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :unity_symbol %>
    <%= form.text_field :unity_symbol %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
