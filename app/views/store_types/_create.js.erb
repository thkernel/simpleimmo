$("ul.errors").html("");
<% if @store_type.errors.any? %>
  <% @store_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".store-types").html("<%= escape_javascript(render 'index') %>");
  $("#store-type-modal").modal("hide");
 
<% end %>