<%= form_with(model: payment_reason, local: true) do |form| %>
  <% if payment_reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_reason.errors.count, "error") %> prohibited this payment_reason from being saved:</h2>

      <ul>
      <% payment_reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
