$("ul.errors").html("");
<% if @material_type.errors.any? %>
  <% @material_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".material-types").html("<%= escape_javascript(render 'index') %>");
  $("#material-type-modal").modal("hide");
 
<% end %>