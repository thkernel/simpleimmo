$("ul.errors").html("");
<% if @store.errors.any? %>
  <% @store.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".stores").html("<%= escape_javascript(render 'index') %>");
  $("#store-modal").modal("hide");
 
<% end %>