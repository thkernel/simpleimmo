$("ul.errors").html("");
<% if @tenant_type.errors.any? %>
  <% @tenant_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".tenant-types").html("<%= escape_javascript(render 'index') %>");
  $("#tenant-type-modal").modal("hide");
 
<% end %>