$("ul.errors").html("");
<% if @city.errors.any? %>
  <% @city.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".cities").html("<%= escape_javascript(render 'index') %>");
  $("#city-modal").modal("hide");
 
<% end %>