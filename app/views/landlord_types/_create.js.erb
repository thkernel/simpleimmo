$("ul.errors").html("");
<% if @landlord_type.errors.any? %>
  <% @landlord_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".landlord-types").html("<%= escape_javascript(render 'index') %>");
  $("#landlord-type-modal").modal("hide");
 
<% end %>