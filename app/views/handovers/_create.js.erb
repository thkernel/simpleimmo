$("ul.errors").html("");
<% if @handover.errors.any? %>
  <% @handover.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".handovers").html("<%= escape_javascript(render 'index') %>");
  $("#handover-modal").modal("hide");
 
<% end %>