$("ul.errors").html("");
<% if @tax.errors.any? %>
  <% @tax.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".taxes").html("<%= escape_javascript(render 'index') %>");
  $("#tax-modal").modal("hide");
 
<% end %>